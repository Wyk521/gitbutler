#!/bin/sh
# RepoScope Desktop has no `but` CLI or post-install filesystem mutation.
# Keep this filename as a harmless compatibility hook for package builders
# that still discover the upstream script; it intentionally does nothing.
exit 0
